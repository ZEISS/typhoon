components:
  requestBodies:
    CreateAccount:
      content:
        application/json:
          schema:
            type: object
            required:
              - operatorId
              - name
            properties:
              operatorId:
                type: string
                format: uuid
              teamId:
                type: string
                format: uuid
              name:
                type: string
              description:
                type: string
