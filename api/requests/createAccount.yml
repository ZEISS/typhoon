components:
  requestBodies:
    CreateAccount:
      content:
        application/json:
          schema:
            type: object
            required:
              - systemId
              - name
            properties:
              systemId:
                type: string
                format: uuid
              name:
                type: string
              description:
                type: string
