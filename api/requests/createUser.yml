components:
  requestBodies:
    CreateUser:
      content:
        application/json:
          schema:
            type: object
            required:
              - accountId
              - name
            properties:
              accountId:
                type: string
                format: uuid
              name:
                type: string
              description:
                type: string
